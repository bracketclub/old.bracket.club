#!/bin/bash

# TODO: Create clone of site (spin up server, follow all links to download all pages, normalize paths)
# TODO: Optionally call bin/dns -s to reset dns for static site
scp -r static/$1/* linode:/home/lkarrys/www/tweetyourbracket.com/public_html/$1
echo "Don't forget to pause the jitsu server!"